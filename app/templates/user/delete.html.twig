{% extends 'base.html.twig' %}

{% block title %}
    {{ 'delete.user'|trans({'%id%': user.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'delete.user'|trans({'%id%': user.id|default('')}) }} {{ user.email }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.delete'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('user_index') }}" title="{{ 'users.link'|trans }}">{{ 'users.link'|trans }}</a>
    </div>
    {{ form_end(form) }}
{% endblock %}