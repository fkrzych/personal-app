{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.event_delete'|trans({'%id%': category.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.event_delete'|trans({'%id%': contact.id|default('')}) }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.delete'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('event_index') }}" title="go to events">{{ 'events.link'|trans }}</a>
    </div>
    {{ form_end(form) }}
{% endblock %}