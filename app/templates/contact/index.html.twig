{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.contact_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.contact_list'|trans }}</h1>
    <a href="{{ url('contact_create') }}" title="create contact">{{ 'create.contact'|trans}}</a>
    {% if pagination is defined and pagination.items|length %}

        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'label.id'|trans, 'contact.id') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.contact_name'|trans, 'contact.name') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.phone'|trans, 'contact.phone') }}</th>
                <th scope="col">
                    {{ 'label.actions'|trans }}
                </th>
            </tr>
            </thead>
            <tbody>
            {% for contact in pagination.items %}
                <tr>
                    <td>{{ contact.id }}</td>
                    <td>{{ contact.name }}</td>
                    <td>{{ contact.phone }}</td>
                    <td>
                        <a href="{{ url('contact_show', {'id': contact.id}) }}" title="show contact">{{ 'show.contact'|trans}}</a>
                        <a href="{{ url('contact_edit', {'id': contact.id}) }}" title="edit contact">{{ 'edit.contact'|trans}}</a>
                        <a href="{{ url('contact_delete', {'id': contact.id}) }}" title="delete contact">{{ 'delete.contact'|trans}}</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>
    {% else %}
        <p>
            {{ 'message.no_data'|trans }}
        </p>
    {% endif %}
    <div class="link-centered">
        <a href="{{ url('event_index') }}" title="go to events">{{ 'events.link'|trans }}</a>
        <a href="{{ url('category_index') }}" title="go to categories">{{ 'categories.link'|trans }}</a>
    </div>
{% endblock  %}